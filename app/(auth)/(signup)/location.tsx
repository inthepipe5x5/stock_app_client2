import LocationFormScreen from "@/screens/(auth)/location";
import { AuthLayout } from "@/screens/(auth)/layout";

export default () => (
  <AuthLayout>
    <LocationFormScreen />;
  </AuthLayout>
);
